# BookStack API Setup Environment Variables
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file: cp .env.setup.example .env.setup
# 2. Create an API token in BookStack: Settings > API Tokens > Create Token
# 3. Fill in the values below
# 4. Set permissions: chmod 600 .env.setup
# 5. NEVER commit .env.setup to git
# =============================================================================

# BookStack instance URL (no trailing slash)
BOOKSTACK_URL=https://learn.lceic.com

# API Token ID (from BookStack Settings > API Tokens)
# This is the numeric ID shown after creating the token
BOOKSTACK_TOKEN_ID=changeme_your_token_id

# API Token Secret (from BookStack Settings > API Tokens)
# This is shown only once when creating the token - save it securely
BOOKSTACK_TOKEN_SECRET=changeme_your_token_secret

# =============================================================================
# Usage in scripts:
#
#   source "${SCRIPT_DIR}/.env.setup"
#   : "${BOOKSTACK_URL:?BOOKSTACK_URL not set}"
#   : "${BOOKSTACK_TOKEN_ID:?BOOKSTACK_TOKEN_ID not set}"
#   : "${BOOKSTACK_TOKEN_SECRET:?BOOKSTACK_TOKEN_SECRET not set}"
#
# API Authentication header:
#   -H "Authorization: Token ${BOOKSTACK_TOKEN_ID}:${BOOKSTACK_TOKEN_SECRET}"
# =============================================================================
